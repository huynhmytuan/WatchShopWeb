@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
                @Html.ActionLink("Tài Khoản", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "nav-link"})
}
else
{
}
